const authRouter = require("./auth");
const homeRouter = require("./home");
const messageRouter = require("./message");
const userRouter = require("./user");

module.exports = { authRouter, homeRouter, messageRouter, userRouter };